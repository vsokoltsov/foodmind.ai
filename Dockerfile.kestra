ARG KESTRA_VERSION=v1.3.30
ARG KESTRA_KUBERNETES_PLUGIN_VERSION=1.9.1

FROM kestra/kestra:${KESTRA_VERSION}

ARG KESTRA_KUBERNETES_PLUGIN_VERSION

USER root
RUN /app/kestra plugins install io.kestra.plugin:plugin-kubernetes:${KESTRA_KUBERNETES_PLUGIN_VERSION}
